// Define an arrow function that takes two parameters: x and y
let myNum = (x, y)=>{
    // Add x and y, store the result in z
    let z = x+y;
    // Log the result to the console

    console.log(z);

}

// Call the function with arguments 2 and 3
myNum(2,3);
